# Local development (uses Vite proxy if unset, or point to local server)
VITE_API_URL=http://localhost:5000/api

# Production backend (also set in .env.production for builds)
# VITE_API_URL=https://launch-layer-backend.onrender.com/api

VITE_WHATSAPP_NUMBER=917752836866
